export {plugin as classesPlugin, config as classesConfig} from './classes';
export {plugin as handlersPlugin, config as handlersConfig} from './handlers';
export {plugin as serverlessPlugin, config as serverlessConfig} from './serverless';
